{
  "name": "proof-ts-lib",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "test": "node --test",
    "lint": "node tools/lint.mjs"
  }
}
