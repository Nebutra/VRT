import { test } from "node:test";
import assert from "node:assert/strict";

test("addition is correct", () => assert.equal(2 + 3, 5));
test("multiplication is correct", () => assert.equal(2 * 3, 6));
